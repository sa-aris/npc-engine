name: CI

on:
  push:
    branches: [ main, master, "claude/**" ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    name: ${{ matrix.os }} / ${{ matrix.compiler }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            compiler: gcc-12
            cc: gcc-12
            cxx: g++-12
          - os: ubuntu-22.04
            compiler: clang-15
            cc: clang-15
            cxx: clang++-15
          - os: macos-14
            compiler: clang
            cc: clang
            cxx: clang++

    steps:
      - uses: actions/checkout@v4

      - name: Install GCC 12 (Ubuntu)
        if: matrix.compiler == 'gcc-12'
        run: sudo apt-get install -y g++-12 liblua5.4-dev

      - name: Install Clang 15 (Ubuntu)
        if: matrix.compiler == 'clang-15'
        run: |
          sudo apt-get install -y clang-15 liblua5.4-dev
          sudo update-alternatives --install /usr/bin/clang clang /usr/bin/clang-15 100
          sudo update-alternatives --install /usr/bin/clang++ clang++ /usr/bin/clang++-15 100

      - name: Install Lua (macOS)
        if: matrix.os == 'macos-14'
        run: |
          brew install lua
          echo "PKG_CONFIG_PATH=$(brew --prefix lua)/lib/pkgconfig" >> $GITHUB_ENV

      - name: Configure
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DNPC_LUA_BRIDGE=ON

      - name: Build
        run: cmake --build build --parallel

      - name: Test
        working-directory: build
        run: ctest --output-on-failure
